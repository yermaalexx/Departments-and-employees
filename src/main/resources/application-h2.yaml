server:
  port: 8080

minimumAgeOfEmployee: 18

spring:
  datasource:
    url: "jdbc:h2:mem:testdb"
    driverClassName: "org.h2.Driver"
    username: name
    password:
  jpa:
    database-platform: "org.hibernate.dialect.H2Dialect"
    open-in-view: false
  h2:
    console:
      enabled: true
